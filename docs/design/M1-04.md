# M1-04 · IPC protocol and the app shell — design brief

> The first screen of the product. Card: [`docs/specs/M1-sound/M1-04.md`](../specs/M1-sound/M1-04.md).
> Language: [`principles.md`](principles.md). Controls: [`../product/controls.md`](../product/controls.md).
> The implementation follows this brief; `ui-quality-reviewer` and `usability-reviewer` check the
> result against the numbers in **Measurements**.

## Job

**Open the app, see what is loaded, press play, hear it at a level that is not too loud, and know
where in the music you are.** Flows: **F0** (set up the sound) and **F1** (first run → music).

Three controls from `principles.md › Controls we will keep needing` are born here — **Transport**,
**Volume**, **Readout** — plus the **Error strip**. Everything else (reference, prompt, timeline,
layers, cue, filter, energy) is M4–M5 and does not appear, not even greyed out.

## References

R15 and R19 in [`docs/research/topics.md`](../research/topics.md) are still `open`, so this brief
rests on primary sources read directly. What was taken and what was rejected:

| Source | Read | Taken | Rejected |
|---|---|---|---|
| Ableton Live 12 Reference Manual, *Arrangement View*, ableton.com (accessed 2026-09-06) — <https://www.ableton.com/en/manual/arrangement-view/> | position shown as `bar.beat.sixteenth`, monospaced, editable in place; the transport is a fixed strip that never scrolls away | musical position as the primary number, counted **from 1**; a transport that is always in the same place | the sixteenth field (nothing in M1 subdivides a beat) and the click-to-type editing (there is nowhere to seek to in M1) |
| Mixxx User Manual 2.3, *DJing With Mixxx* and *Mixxx Controls*, manual.mixxx.org (accessed 2026-09-06) — <https://manual.mixxx.org/2.3/en/chapters/djing_with_mixxx.html> | master gain is one control near the output, and the manual tells people to leave it alone and set level at the speakers | the output group is a **separate region** from the transport and from the document, and its default is conservative | the 0.0–1.0–5.0 linear pot: it can boost, and a listener has no meter to judge with. Our range stops at 0 dB — the output stage attenuates, never amplifies |
| Native Instruments Traktor gain-staging guidance, digitaldjtips.com and inMusic support (accessed 2026-09-06) — <https://support.inmusicstore.com/en/support/solutions/articles/69000879489-digital-djing-and-sound-quality-handling-levels-in-traktor> | working headroom of −6 to −12 dB is the routine habit, not an edge case | **−12 dB** as the safe first-run value is a number DJs already recognise as "quiet but working", not an arbitrary hush | per-deck gain and a headroom setting: one master value is all M1 has |
| Chrome for Developers, *Change the destination output device in Web Audio* (`AudioContext.setSinkId`) — <https://developer.chrome.com/blog/audiocontext-setsinkid> and MDN *MediaDevices.enumerateDevices()* (accessed 2026-09-06) | `setSinkId` takes only ids that `enumerateDevices()` produced; in a **browser**, labels stay empty until the microphone permission is granted | the id constraint, which is what makes the renderer the only possible source of the device list | the empty-label rule itself — it is a browser rule and **does not hold in this runtime**. Measured in Electron 44.2.0: 9 `audiooutput` entries with real ids and real labels, no prompt (`docs/memory/measurements.md`, 2026-09-06). The picker is therefore designed for the **named** case, with no opt-in |
| Spotify Connect device picking, community documentation (accessed 2026-09-06) — <https://community.spotify.com/t5/Desktop-Windows/Desktop-Select-default-Sound-Device/td-p/4722652> | the currently active device is marked in the list, and the picker is reachable without leaving playback | the current device name is **visible at rest**, not only inside the open list | burying the picker in a settings menu — F0 says the device in use must be visible |
| W3C WCAG 2.2, SC 2.5.8 *Target Size (Minimum)*, AA — <https://wcag22aa.org/new-criteria/target-size/> (accessed 2026-09-06) | every pointer target at least 24 × 24 CSS px, or 24 px of offset to its neighbours | the 24 px floor is the hard minimum in **Measurements**; the transport is far above it | nothing |

## Layout

### Regions

Four, top to bottom, in one full-height flex column. Each region lays its content inside the same
**content column**: `width: min(100% - 2 × var(--space-6), 1200px); margin-inline: auto`.

| Region | Element | Job | Height |
|---|---|---|---|
| `header` | `<header>` | the document: name · tempo · key | 64 px, fixed |
| `notice` | `<div>` | the error strip, when there is one | **out of flow** — see below |
| `stage` | `<main>` | the readout, the beat indicator, the transport, the status line | fills the rest |
| `output` | `<footer>` | volume · mute · output device | 88 px, fixed |

`stage` centres its content on both axes. `output` is pinned to the bottom and never scrolls away —
stop and mute are always reachable (`principles.md › What we do not do`).

**`notice` is `position: absolute`, anchored to the top of `stage`, and takes no space.** The shell
is `position: relative`; the strip sits at `top: var(--space-4)` inside the stage box, in the
content column. This is the one thing on the screen that appears while music is playing, and if it
were in flow it would shrink `stage` and slide Play, Stop and Mute out from under a hand that is
reaching for exactly them — a device lost mid-set is precisely when someone lunges for Stop.
**Nothing moves when the strip appears or is dismissed.**

Out of flow is not out of the DOM: `notice` stays between `header` and `stage` in source order, so
the focus order below is unaffected.

It cannot cover the readout, and the arithmetic says so. `stage` content is
64 (readout line) + 8 + 16 (dots) + 32 + 56 (transport) + 24 + 24 (status) = **224 px**, centred.
The strip occupies 16 (gap below header) + 104 (its own height: 16 + 28 + 12 + 32 + 16) + 24
(clearance) = **144 px** from the top of `stage`. It clears the content while
`(H − 224) / 2 ≥ 144`, i.e. `H ≥ 512`, i.e. a window at least **664 px** tall. Set
`minHeight: 680` on the window in `apps/desktop/src/main/window.ts` — the app opens at 900 and the
floor makes the overlap unreachable by resizing. A second concurrent error is not designed: the
strip shows one at a time, the newest.

### Text wireframe (1440)

```
┌───────────────────────────────────────────────────────────────────────────┐
│ header                                                                    │
│  Example set                                    128 BPM  ·  A minor       │
├───────────────────────────────────────────────────────────────────────────┤
│ stage                                                                     │
│                                                                           │
│  notice   (out of flow, over stage's empty top; absent at rest)           │
│  ▌ The output device is gone. Sound moved to the system default.          │
│  ▌                                              [ Choose a device ]  [×]  │
│                                                                           │
│                              03:2                                         │
│                          ●  ○  ○  ○                                       │
│                                                                           │
│                     [  ▶ Play  ]   [  ■ Stop  ]                           │
│                                                                           │
│                     Playing · bar 3 of 16                                 │
│                                                                           │
├───────────────────────────────────────────────────────────────────────────┤
│ output                                                                    │
│  Volume ▬▬▬▬▬▬▬▬▬●───────  −12 dB   [ 🔇 Mute ]   Output [ System ▾ ]     │
└───────────────────────────────────────────────────────────────────────────┘
```

### What changes with width

| | 1024 | 1440 | 1920 |
|---|---|---|---|
| content column | 960 px (fluid: viewport − 2 × 32) | 1200 px | 1200 px |
| side gutters | 32 px | 120 px | 360 px |
| readout size | `--text-8` | `--text-8` | `--text-8` |
| transport button size | identical | identical | identical |
| `output` row | one row | one row | one row |
| volume slider track | 240 px | 240 px | 240 px |

**Nothing moves at all.** With the device `<select>` fixed at 260 px the output row is identical at
the three widths: 52 + 12 + 240 + 12 + 96 + 24 + 104 + 24 + 52 + 12 + 260 = **888 px**, which fits
the narrowest content column (960) with 72 px to spare. The type scale is not responsive, the
readout is not responsive, and every control keeps the same pixel size at all three widths. Reason: the three `shot` states are compared
across widths, and a screen with one number and three controls has no reason to reflow. Above
1200 px the design spends the extra space on emptiness, which is what "a booth at night, not a
spreadsheet" buys.

Below 1024 the layout is not designed (`CLAUDE.md › Quality targets`: desktop only, no mobile).
The window opens at 1440 × 900 (`apps/desktop/src/main/window.ts`).

## Tokens

This card writes `apps/desktop/src/renderer/tokens.css`. The gate is that **nothing on screen sits
outside these values**. Tailwind v4 reads them through `@theme inline`, so a theme swap is a change
of custom properties on `:root`, not a rebuild:

```
:root { --ui-surface: #0D0F12; … }                       /* dark, the default */
@media (prefers-color-scheme: light) { :root { --ui-surface: #FBFBFC; … } }
[data-theme="dark"]  { --ui-surface: #0D0F12; … }        /* the Settings override, M1-04 ships the hook */
[data-theme="light"] { --ui-surface: #FBFBFC; … }
@theme inline { --color-surface: var(--ui-surface); … }
```

### Colour — dark (the default)

Contrast measured with the WCAG 2.x relative-luminance formula, 2026-09-06.

| Role | Value | vs `surface` | vs `surface-2` | Used for |
|---|---|---|---|---|
| `--color-surface` | `#0D0F12` | — | — | page |
| `--color-surface-2` | `#171A20` | 1.10 | — | `output`, the error strip, the select |
| `--color-ink` | `#EDEFF2` | **16.66** | **15.13** | the readout, the document name, button labels, the focus ring |
| `--color-muted` | `#9BA4B0` | **7.61** | **6.91** | BPM, key, the status line, control labels |
| `--color-line` | `#626A78` | **3.52** | **3.20** | control borders, the slider track, separators |
| `--color-accent` | `#FFB454` | **10.88** | **9.88** | Play fill, slider fill, the current beat |
| `--color-accent-ink` | `#20160A` | — | — | text on `accent` — **10.09** against it |
| `--color-signal-ok` | `#5FD08A` | **9.94** | **9.03** | reserved (soundcheck, M5) |
| `--color-signal-warn` | `#F2C14E` | **11.44** | **10.38** | the Mute active fill — `accent-ink` on it is **10.60** |
| `--color-signal-error` | `#FF7A6B` | **7.54** | **6.84** | the error strip's rule and title |

### Colour — light

| Role | Value | vs `surface` | vs `surface-2` |
|---|---|---|---|
| `--color-surface` | `#FBFBFC` | — | — |
| `--color-surface-2` | `#EEEFF3` | 1.11 | — |
| `--color-ink` | `#14171C` | **17.37** | **15.63** |
| `--color-muted` | `#575F6C` | **6.23** | **5.61** |
| `--color-line` | `#7E8592` | **3.59** | **3.23** |
| `--color-accent` | `#A34A00` | **5.74** | **5.17** |
| `--color-accent-ink` | `#FFFFFF` | — | **5.94** against `accent` |
| `--color-signal-ok` | `#0E6B45` | **6.33** | **5.70** |
| `--color-signal-warn` | `#7A5200` | **6.69** | **6.02** — white on it is **6.92** |
| `--color-signal-error` | `#B3261E` | **6.32** | **5.69** — white on it is **6.54** |

Floors held: text ≥ 4.5:1, UI edges and large text ≥ 3:1. The lowest figure in the table is
`line` at **3.20**, and `line` never carries text.

`apps/desktop/src/main/window.ts` sets `backgroundColor: '#0b0b0f'`; change it to `#0D0F12` so the
window does not flash a colour the stylesheet does not know. That hex lives in `main`, outside the
renderer CSS the grep gate scans.

### Space — 4 px base, exactly as `principles.md`

`--space-1` 4 · `--space-2` 8 · `--space-3` 12 · `--space-4` 16 · `--space-5` 24 · `--space-6` 32 ·
`--space-7` 48 · `--space-8` 64. No other spacing value appears anywhere.

### Radius

`--radius-1` 4 px (the slider track, the beat dots' container) · `--radius-2` 8 px (buttons, the
select, the error strip) · `--radius-3` 12 px (reserved for cards, M4). `--radius-pill` 9999 px,
only for the mute toggle and the slider thumb — and note that on a 40 px-tall element the effective
radius is 20 px, not 9999.

### Type

No font is bundled in M1 (see **Open questions**). Three roles, system stacks:

```
--font-body:    ui-sans-serif, system-ui, "Inter", "Helvetica Neue", Arial, sans-serif
--font-display: same stack as --font-body, distinguished by weight 600 and --tracking-tight
--font-mono:    ui-monospace, "SF Mono", "JetBrains Mono", Menlo, Consolas, monospace
--tracking-tight: -0.02em
```

Scale, ratio 1.25 from a 13 px floor (`principles.md`: no text under 13 px). Line height in
parentheses:

| Token | px | Used for |
|---|---|---|
| `--text-1` | 13 (18) | control labels ("Volume", "Output"), the strip's action |
| `--text-2` | 16 (24) | body, the status line, the select |
| `--text-3` | 20 (28) | the document name, transport button labels, the error title |
| `--text-4` | 25 (32) | the dB value |
| `--text-5` | 32 (40) | reserved |
| `--text-6` | 40 (48) | reserved |
| `--text-7` | 50 (56) | reserved |
| `--text-8` | 62 (64) | the `bar:beat` readout |

`font-variant-numeric: tabular-nums` on: the readout, the dB value, the BPM.

### Motion

`--duration-1` 120 ms · `--duration-2` 240 ms · `--ease-out` `cubic-bezier(0.16, 1, 0.3, 1)` ·
`--ease-in-out` `cubic-bezier(0.4, 0, 0.2, 1)`. `--duration-beat` is not a static token: it is
`60000 / bpm` ms, set on the beat indicator as an inline custom property from the score's tempo
(468.75 ms at 128 BPM).

### Nothing new proposed

Every role above already exists in `principles.md`. `--radius-pill`, `--tracking-tight` and the
type scale are the concrete values of roles that file names in words. No token is added.

## Components and states

Element ids below are the `data-testid` values the `shot` script measures.

### 1 · Header — `document-name`, `document-tempo`, `document-key`

Left: the document name, `--font-display`, `--text-3`, `--color-ink`. Right: `128 BPM` then a
separator `·` in `--color-line` then `A minor`, both `--text-2` `--color-muted`, the digits in
`--font-mono` with `tabular-nums`.

The score carries no name field (`packages/score/src/schema.ts`: `lineage.label` is optional).
Copy: `score.lineage?.label` when present, otherwise **"Example set"** — the words
`usability.md › The empty state is a state` already uses. Key copy: `"A minor"` — tonic then mode,
lower case mode, never `Am`.

| State | Header shows |
|---|---|
| loading (no `score:load` yet) | name `Loading the set…` in `--color-muted`; tempo and key each `—` |
| loaded | name · `128 BPM` · `A minor` |

### 2 · Readout — `readout`, `beat-indicator`

`--font-mono`, `--text-8`, `--color-ink`, `tabular-nums`, `--tracking-tight`.

Format: `bar` padded to two digits, a colon, `beat` — **one-based**, so a stopped transport reads
`01:1` and never `00:0`. The colon is `--color-muted`. The padding is what prevents the shift at
bar 10; `tabular-nums` alone does not, because the digit **count** changes.

The readout is driven by a `requestAnimationFrame` loop reading `engine.position()` in the
renderer — **not** by `transport:position`, which only fires once per bar and would freeze the beat
digit for a whole bar. IPC carries the bar to `main`; the screen reads the engine directly.

Beat indicator: `score.meter.beatsPerBar` dots (4) in a row under the readout. Each dot sits in a
fixed 16 px box; the dot is 8 px `--color-line` at rest and 14 px `--color-accent` on the current
beat, so the state is carried by size as well as colour. Transition `--duration-1` `--ease-out`.

| State | Readout | Dots |
|---|---|---|
| loading | `—:—` in `--color-muted` | all `--color-line`, no dot active |
| stopped | `01:1` in `--color-ink` | dot 1 active, static |
| playing | advancing | the current beat active |
| ended (16 bars over) | back to `01:1` | dot 1 active |

### 3 · Transport — `transport-play`, `transport-stop`

Two buttons, `--space-3` apart. Two, not one toggling button: `principles.md` says stop is
deliberate; a label that swaps between "Play" and "Stop" changes the button's width and moves the
primary target under the cursor; and when Pause arrives in M4 the group becomes Play/Pause + Stop
without moving anything.

**Play** — primary. `--color-accent` fill, `--color-accent-ink` label, `--radius-2`, height 56 px,
min-width 148 px, `--text-3`, a 20 px triangle glyph then the word `Play` with `--space-2` between.
**Stop** — secondary. Transparent fill, 1 px `--color-line` border, `--color-ink` label, same
height, min-width 132 px, a 20 px square glyph then the word `Stop`.

| State | Play | Stop |
|---|---|---|
| rest (stopped) | accent fill, enabled | `disabled`, label and border drop to `--color-line` |
| rest (playing) | `disabled`, fill drops to `--color-surface-2`, label to `--color-line` | enabled |
| hover | fill lightens by the theme's next step — dark `#FFC06B`, light `#8E4000`; Stop's border goes to `--color-ink` | same rule |
| focus | `outline: 2px solid var(--color-ink); outline-offset: 2px` | same |
| active (pressed) | `transform: translateY(1px)`, no colour change | same |
| disabled | as above, plus `cursor: default`; the **reason** is in `status-line`, never a bare dead button | same |
| loading | Play disabled, `status-line` reads `Waiting for the set.` | disabled |
| error | unchanged — an error never blocks the transport | unchanged |

The glyphs are inline SVG in `currentColor` inside `inline-flex items-center justify-center`;
they are decorative (`aria-hidden`), the word carries the name.

### 4 · Status line — `status-line`

One line of `--text-2` `--color-muted` under the transport, `--space-5` below it. It is the screen's
`aria-live="polite"` region and the place every "why is this disabled" sentence lives. Fixed
height (24 px) so nothing jumps when it changes.

| Situation | Copy |
|---|---|
| loading, under 5 s | `Waiting for the set.` |
| loading, past 5 s | — the strip takes over, see below |
| stopped, ready | `Ready. Press play.` |
| playing | `Playing · bar 3 of 16` |
| playing, muted | `Playing · bar 3 of 16 · muted` |
| ended | `The set reached the end.` |
| no output device | `No output device. Connect speakers or headphones.` |

While playing, the line is rewritten only when the **bar** changes, not every beat — a live region
updated 128 times a minute is unusable with a screen reader.

### 5 · Volume — `output-volume`, `output-volume-value`

A native `<input type="range">`, `min="-60"`, `max="0"`, `step="1"`, so arrows, `Home`, `End`,
`PageUp` and `PageDown` come from the platform and need no code. Label `Volume`, `--text-1`
`--color-muted`, to its left with `--space-3` between.

Track: 240 × 4 px, `--radius-1`, `--color-line`. Fill (left of the thumb): `--color-accent`.
Thumb: 16 px circle, `--color-ink`, with a 2 px `--color-surface-2` ring — the ring is what makes
the thumb legible against the accent fill, since `ink` on `accent` in dark is only 1.53:1 while
`ink` on `surface-2` is 15.13 and `accent` on `surface-2` is 9.88. The input's own box is 32 px
tall so the pointer target clears 24 px.

Value: `--font-mono`, `--text-4`, `--color-ink`, `tabular-nums`, fixed width 96 px, right-aligned.
Format `−12 dB` with a real minus sign (U+2212), integers only, and `−60 dB` renders as
`−∞ dB` — at the bottom of the range the output is silent and the number should say so.

- **First run: −12 dB** (`SAFE_OUTPUT_GAIN_DB`). Restore rule, in two sentences: a stored value
  that parses and sits inside `OUTPUT_GAIN_DB` (−60…0) is restored **exactly as written**; anything
  missing, corrupt, of the wrong type or out of range falls back to **−12 dB**, never to the top of
  the range. The app therefore never comes back louder than it was left (`usability.md ›
  heuristic 3`), because the only value it can come back to is one the person set. Stored in
  `app.getPath('userData')`, never in the document.
- **`0`–`9`** set the value to `−60 + 6n` dB: `0` → −60 (silent), `5` → −30, `9` → −6. The top of
  the range, 0 dB, is reachable only by dragging or by `End` — a stray number key can never jump
  the output to full.

| State | Look |
|---|---|
| rest | fill `--color-accent` |
| hover | thumb grows to 18 px, `--duration-1` |
| focus | ring `2px --color-ink` at `outline-offset: 2px` around the whole input box |
| dragging | the dB value stays visible and updates on every step — the number is the readout of the gesture |
| muted | fill drops to `--color-line`, the value stays at its dB number and the word `muted` appears in `status-line`; the slider stays enabled, so lowering it while muted and then unmuting is quiet, not loud |
| disabled | never — volume works even before the score arrives |

### 6 · Mute — `output-mute`

A `<button>` with `aria-pressed`, 40 px tall, `--radius-pill`, padding-inline `--space-4`,
a 16 px speaker glyph plus a word.

| State | Fill | Border | Label | Glyph |
|---|---|---|---|---|
| off (rest) | transparent | 1 px `--color-line` | `Mute` | speaker |
| off (hover) | `--color-surface-2` | `--color-ink` | `Mute` | speaker |
| on | `--color-signal-warn` | none | `Muted` | speaker with a slash |
| focus | unchanged fill | `outline: 2px solid var(--color-ink); outline-offset: 2px` | | |

The label text changes, so the state is never carried by colour alone. `aria-pressed="true"` when
muted. Muting does not stop the transport: `bar:beat` keeps advancing (the card's gate).

### 7 · Output device — `output-device`

A native `<select>`, 40 px tall, `--radius-2`, 1 px `--color-line`, `--color-surface-2`,
`--text-2`, width 260 px at every breakpoint, `text-overflow: ellipsis`. Label `Output`, `--text-1`
`--color-muted`, to its left. Native, not a custom popover: it inherits keyboard, screen-reader and
platform behaviour, and the popover traps listed in `CLAUDE.md › UI` do not apply.

First option is always `System default` with value `'default'` — Chromium's well-known unsalted
id, and the value `FIRST_RUN.deviceId` already holds. Then one option per
`enumerateDevices()` entry of kind `audiooutput`. The chosen device's name is visible at rest in
the closed select — F0 requires the device in use to be on screen.

| State | Select shows |
|---|---|
| rest — the ordinary case | `System default` and one named option per device, the current one selected |
| only `System default` came back | that one option, `disabled`, since there is nothing to choose between: one output, or a runtime that hides labels the way a browser does. `status-line` is untouched — this is not a fault, and nothing is missing that the person could act on |
| no `audiooutput` at all | one `disabled` option `No output device`; Play is disabled and `status-line` gives the reason |
| the device disappears while playing | falls back to `System default`; the `device-lost` strip appears; playback does not stop |
| switching | the selection applies at once (`setSinkId`); no confirmation, no restart |
| focus | `outline: 2px solid var(--color-ink); outline-offset: 2px` |

There is **no permission flow and no opt-in**. Measured in this runtime, `enumerateDevices()`
returns real ids and real labels on first paint with no `getUserMedia` call and no macOS TCC dialog
(`docs/memory/measurements.md`, 2026-09-06). The renderer enumerates once on mount and again on
every `devicechange`. Nothing on the first screen asks the person for anything.

**Why the renderer owns the list, and `main` only the choice.** Enumerating in `main` was
considered: this is Electron, `main` runs Node, and `output:device` is shaped as though the list
came from that side. The names are genuinely available there — `system_profiler SPAudioDataType`
returns them on macOS with no permission (checked 2026-09-06 on this machine: `LG ULTRAWIDE`,
`SAMSUNG`, `BlackHole 2ch`). **The blocker is the id, not the name:** `setSinkId` accepts only the
ids `enumerateDevices()` produced, and Chromium builds each as a hash of the origin, the real device
id and a per-profile salt, resolvable back to a device **only inside Chromium**
([MediaDeviceSalt component](https://chromium.googlesource.com/chromium/src/+/main/components/media_device_salt/README.md),
accessed 2026-09-06). An OS-side name cannot be matched to an id the renderer can select with, so a
picker built from `main` could list devices it is unable to switch to — worse than listing none.
`'default'` is the one unsalted exception, which is why `System default` always works.

So the **renderer** produces the list and `main` stores the choice: `output:device` returning
`{ devices, selected }` can only echo a list the renderer gave it.

**Real labels are long, so the select truncates.** The measured names run to
`Alto-falantes (MacBook Pro) (Built-in)` and `Microsoft Teams Audio Device (Virtual)` — past what
260 px of `--text-2` holds. The closed select ellipsises and carries a `title` with the full name;
the open list is the platform's and shows the name in full. The select is fixed at 260 px at all
three widths and **never grows to fit a name**, or the output row would reflow every time someone
plugged in an interface.

### 8 · Error strip — `error-strip`

Full content-column width, `--color-surface-2` background, a 3 px `--color-signal-error` rule on
the left edge, `--radius-2`, padding `--space-4`, height 104 px, `--space-4` below the header, out of
flow (see **Regions**). Title `--text-3`
`--color-ink`; one action button (`--text-1`, outline, 32 px tall) and a dismiss `×` (32 × 32,
accessible name `Dismiss`). `role="alert"`.

It is never a modal and never covers the transport (`principles.md`). Copy, in the user's words,
mapped from `EngineErrorCode` (`packages/engine/src/errors.ts`):

| Code | Title | Action |
|---|---|---|
| `invalid-score`, `automation-*`, `unknown-*`, `unsupported-fx` | `This set could not be played.` | `Try again` |
| `invalid-audio`, `context-in-use`, `context-not-offline` | `The audio engine could not start.` | `Try again` |
| `score-never-arrived` — no `score:load` within 5 s of the window painting | `The set did not load.` | none in M1, dismiss only — see **Open questions 4** |
| `device-lost` — the selected device vanished while in use (raised by the renderer on `devicechange`) | `The output device is gone. Sound moved to the system default.` | `Choose a device` — moves focus to `output-device` |
| `sink-unavailable` — the audio context cannot choose a device at all (`EngineError`, thrown when the raw context has no `setSinkId`) | `This audio setup cannot switch outputs. Sound is playing on the system default.` | none, dismiss only — there is nothing to choose from, so `Choose a device` would be a dead end |
| `schema-refused` — a channel payload fails its Zod schema in the preload | `Something the app sent was not understood, so it was not applied.` | none, dismiss only |

The raw code and message are never shown; they go to the console and to `main`'s log.

### The full state table

Rows the card names are marked ✔; rows `completeness.md` demands and the card does not name are
marked ➕ and designed here anyway.

| State | Header | Readout | Transport | Output group | Notice |
|---|---|---|---|---|---|
| ➕ loading | `Loading the set…` / `—` / `—` | `—:—` muted | both disabled, reason in `status-line` | all live | — |
| ➕ loading timed out (5 s) | `Loading the set…` / `—` / `—` | `—:—` muted | both disabled | all live | strip: `The set did not load.` |
| ✔ stopped | full | `01:1` | Play on, Stop off | all live | — |
| ✔ playing | full | advancing | Play off, Stop on | all live | — |
| ✔ error | unchanged | unchanged | unchanged | unchanged | strip |
| ➕ ended | full | `01:1` | Play on, Stop off | all live | — |
| ➕ no output device | full | `01:1` | Play disabled with a reason | select shows `No output device` | strip |
| ➕ device lost while playing (`device-lost`) | full | keeps advancing | keeps playing | select back to `System default` | strip |
| ➕ context cannot switch outputs (`sink-unavailable`) | full | per transport | per transport | the select is `disabled` | strip |
| ➕ mute while playing | full | keeps advancing | Play off, Stop on | Mute pressed, slider fill `line` | — |
| ➕ only `System default` enumerated | full | per transport | per transport | the select holds one `disabled` option | — |

### What `completeness.md` asks for and this screen does not have

Said plainly, per the rule that a brief omitting a row says why:

- **"Anything that plays over time expects pause, what comes next, and elapsed."** No pause: it
  lands in M4 (`controls.md › Transport`). No "next" and no elapsed: there is nothing after the
  16-bar fixture in M1, and elapsed time has no consumer until the timeline (M5). The transport
  group is laid out so Pause slots between Play and Stop without moving them.
- **"Any list expects reorder, remove, an empty state."** The device `<select>` is a picker, not a
  list the person owns; there is nothing to reorder or remove. Its empty case is designed above.
- **"Settings: one place, grouped."** No Settings panel in M1. Theme follows the system, reduced
  motion follows the system, and the safe start volume is fixed at −12 dB. `tokens.css` ships the
  `[data-theme]` hook so the panel, when it arrives, changes nothing visual.
- **"Anything that takes a while expects progress, an estimate, cancel, and an explicit error with
  retry."** Loading the fixture is the only wait on this screen, and it is a local file read across
  one IPC message: fast enough that a progress bar would flash and lie, and there is nothing to
  cancel. What the row really demands is that the wait **ends**, so it does: if `score:load` has
  not arrived **5 s** after the window paints, `status-line` gives way to the error strip reading
  `The set did not load.` The screen never sits on `Waiting for the set.` forever.
- **"Any decision the system makes expects a why."** M1 makes no decisions; the brain is M4.

## Motion

| What | Duration | Easing | Meaning |
|---|---|---|---|
| beat dot fill and size | `--duration-1` | `--ease-out` | the beat — the one thing tied to the music's clock |
| button hover fill, thumb growth | `--duration-1` | `--ease-out` | affordance |
| transport enabled ↔ disabled | `--duration-2` | `--ease-in-out` | a state change |
| error strip enter | `--duration-2` | `--ease-out` | opacity 0→1 and `translateY(-4px)`→0 — no height animates, because the strip is out of flow and nothing reflows |
| the readout digits | none | — | numbers never animate; they would smear |

`@media (prefers-reduced-motion: reduce)`: every transition above becomes `0s`. The beat dot still
changes which dot is filled — the bar tick becomes a static indicator, exactly as `principles.md`
requires — it simply changes without a transition.

## Keyboard and accessibility

### Focus order

Focus order **is** DOM order — `header` → `notice` → `stage` → `output` — and no element carries a
positive `tabindex`. Written out:

| At rest (no strip) | With the error strip showing |
|---|---|
| `transport-play` | `error-strip` action |
| `transport-stop` | `error-strip` dismiss |
| `output-volume` | `transport-play` |
| `output-mute` | `transport-stop` |
| `output-device` | `output-volume` |
| | `output-mute` |
| | `output-device` |

Five focusable elements at rest, seven with the strip. A `disabled` `<select>` drops out of the
order, so on a machine with one output the last stop is `output-mute`.

So the strip's two controls come **first** while it is showing, and the first `Tab` reaches Play as
soon as it is dismissed. That is the point of keeping `notice` above `stage` **in source order**
even though it is painted out of flow: the thing that went wrong is one `Tab` away, and the fix
costs no `tabindex`.

When the strip appears, focus does not move (it is `role="alert"`, so it is announced where it is).
An unasked focus jump while music plays is worse than an extra `Tab`.

### The map

| Key | Does | Scope |
|---|---|---|
| `Space` | Play when stopped, Stop when playing | global, under the guard below |
| `M` | toggles mute | global, under the guard below |
| `↑` / `↓` | volume ±1 dB | global, under the guard below |
| `0`–`9` | volume to `−60 + 6n` dB | global, under the guard below |
| `Tab` / `Shift+Tab` | focus order above | native |
| `Home` / `End` | volume to −60 / 0 dB | slider focused (native) |
| `PageUp` / `PageDown` | volume ±6 dB | slider focused (native) |
| `Enter` / `Space` | activates the focused button | native |

**One guard, written in terms of what has focus.** The rule in one sentence:

> The global handler returns early — doing nothing, preventing nothing — whenever the key it
> received already **does something native** on `document.activeElement`. The focused element
> always wins.

Only three elements on this screen are focusable, so the whole rule is this table. `·` means the
key is not native there and the global handler acts:

| Focused | `Space` | `↑` / `↓` | `0`–`9` | `M` |
|---|---|---|---|---|
| nothing (`<body>`) | · | · | · | · |
| `<button>` (transport, mute, strip) | native: activates | · | · | · |
| `<select>` (`output-device`) | native: opens | native: steps options | native: type-ahead | native: type-ahead |
| range input (`output-volume`) | · | native: ±1 dB, same result | · | · |

The blanket form — "return early whenever a button, a select or the range input has focus" — is
**wrong**, and the brief does not say it: the moment someone presses Play, focus sits on a
`<button>`, and `M`, `↑`, `↓` and the digits would all go dead. The card requires those keys to
work without the mouse, so the guard has to be per key, not per element.

Three traps the table dodges, all of which pass every unit test while being broken on screen:

1. **`Space` on a focused `<button>` fires twice** — the button's own activation, then the global
   handler toggling the transport back.
2. **`Space` on the focused `<select>`** opens the picker *and* would start or stop the music.
   `output-device` is not a `<button>`, so a guard written only against buttons misses it.
3. **`↑`/`↓` and `0`–`9` on the focused `<select>`** are the platform's option stepping and
   type-ahead; without the guard, choosing a device would also move the volume. The same applies to
   `↑`/`↓` on the focused range input, whose native step is already ±1 dB.

When the handler does act, it calls `preventDefault()` — for `Space`, so the window never scrolls.

### Names and roles

| Element | Accessible name | Notes |
|---|---|---|
| `readout` | `Position` | `aria-label`, value read as text; **not** a live region — announcing every beat is unusable |
| `beat-indicator` | — | `aria-hidden="true"`; the readout already carries the value |
| `transport-play` / `transport-stop` | `Play` / `Stop` | the visible word is the name |
| `output-volume` | `Volume` | `<label for>`; `aria-valuetext="minus 12 decibels"` so a screen reader does not read `-12` as a bare number |
| `output-mute` | `Mute` | `aria-pressed`; the label becomes `Muted` when on |
| `output-device` | `Output device` | `<label for>` |
| `status-line` | — | `aria-live="polite"`, `aria-atomic="true"`, updated on bar change and on state change only |
| `error-strip` | — | `role="alert"` |

Focus indicator everywhere: `outline: 2px solid var(--color-ink); outline-offset: 2px`. Its
adjacent colour is always a surface (the outline sits outside the control), so contrast is
16.66:1 in dark and 17.37:1 in light — well past the 3:1 of **SC 1.4.11 Non-text Contrast**, which
with **SC 2.4.7 Focus Visible** is what AA asks for. Nothing overlays a focused control, so
**SC 2.4.11 Focus Not Obscured (Minimum)** holds too. The change-of-contrast requirement is
**SC 2.4.13 Focus Appearance**, which is AAA and not a target here, though a 2 px offset ring at
these ratios meets it. Never `outline: none`.

No information by colour alone anywhere: mute has a word and a slashed glyph; the transport's
disabled state has a sentence in `status-line`; the beat dot changes size as well as colour; the
error strip has a title, not just a red rule.

## Measurements

What `pnpm --filter desktop shot <state>` records and `ui-quality-reviewer` checks, at 1024 / 1440 /
1920. All values in CSS px.

| Element | Measurement | Value |
|---|---|---|
| content column | `width` | 960 / 1200 / 1200 |
| `header` | `height` | 64 |
| `output` | `height` | 88 |
| `readout` | `font-size` | 62 |
| `readout` | `font-variant-numeric` | contains `tabular-nums` |
| `readout` | `width` at bar 1 vs bar 16 | identical, tolerance 0 |
| `beat-indicator` dot box | `width` / `height` | 16 / 16 |
| `transport-play` | `height` × `min-width` | 56 × 148 |
| `transport-stop` | `height` × `min-width` | 56 × 132 |
| Play ↔ Stop | gap | 12 |
| `output-volume` | box `height` | 32 |
| `output-volume` track | `width` | 240 |
| `output-volume-value` | `width` | 96 |
| `output-mute` | `height` | 40 |
| `output-device` | `height` × `width` | 40 × 260 at all three widths |
| `output-device` | `width` with the longest label selected | still 260 — the select truncates, it never grows |
| every focusable element | `height` and `width` | ≥ 24 (SC 2.5.8) |
| any two adjacent targets | gap | ≥ 12, and ≥ 24 of offset centre-to-centre |
| every text element | `font-size` | ≥ 13 |
| focus ring | `outline-width` / `outline-offset` | 2 / 2 |
| all of the above, at rest and in `playing` | position `left`/`top` | identical between `stopped` and `playing` — pressing Play must not move anything |
| `transport-play`, `transport-stop`, `output-mute`, `readout` | position `left`/`top` with and without `error-strip` | identical, tolerance 0 — an error must not move a control someone is reaching for |
| `error-strip` | `height` | 104 |
| window | `minHeight` | 680 |

Automatable gates the card already names, restated in design terms:

- `grep -nE '#[0-9a-fA-F]{3,8}|[0-9]+px|[0-9.]+rem' apps/desktop/src/renderer --include='*.css' --include='*.tsx'` returns nothing outside `tokens.css`.
- Every colour pair in the tables above recomputed from `tokens.css` and asserted against its
  figure; the floor is 4.5 for text and 3.0 for `line` and the accent fill.

## Open questions

1. **The document's name.** `Score` has no name field; `lineage.label` is optional and the fixture
   does not set it. Options: (a) render `lineage.label ?? "Example set"` — the brief's default,
   and the words `usability.md` already uses; (b) add a required `name` to the score schema —
   a change to `@liminal/score`, outside this card; (c) show the score `id`. Taking (a) unless
   told otherwise.
2. **Bundling a display face.** `principles.md` asks for "a display face with character for numbers
   that matter". M1 ships system stacks, so the readout is a system sans rather than a face chosen
   for its digits. Options: (a) stay on system stacks through M5 and revisit at packaging;
   (b) vendor a variable subset of an OFL face now (adds a licensed asset to the repo — an owner
   decision under process §15). Taking (a) for this card.
3. **`0`–`9` mapping.** `−60 + 6n` dB is chosen so no number key can jump to full output. The
   familiar alternative is percent-of-range, which makes `9` ≈ −6 dB and `0` = silence too, but
   invites `0` being read as "zero dB". Taking the dB formula and printing it in the map.
4. **Retrying a load that never arrived.** The `score-never-arrived` strip has no action, because
   `score:load` is `mainToRenderer` and the renderer has no way to ask for it again — the card
   defines no such channel. Options: (a) dismiss-only, as designed, and the person reopens the app;
   (b) add a `score:request` channel (renderer → main) so `Try again` can exist, which widens the
   card's protocol; (c) have `main` resend on a timer. Taking (a) for this card and flagging (b) as
   the natural fix when a second document source arrives in M2.
5. **Design review.** This screen is new. `docs/design/README.md` marks it `awaiting design: ok` —
   the owner's approval on the brief is awaited before the shell is built. A design canvas of the
   three states at 1440 can be published on request.
