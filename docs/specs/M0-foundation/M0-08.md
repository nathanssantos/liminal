---
id: M0-08
title: Agent instructions loading
milestone: M0
area: docs
priority: P1
depends_on: [M0-01]
listening: false
issue: 8
sync:
  hash: 22bb5335cf9a86ac
  at: 2026-09-05T23:34:49Z
  status: Done
---

## Context

`AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, the eight agents in `.claude/agents/` and the six skills in
`.claude/skills/` (`liminal`, `queue`, `start`, `review`, `deliver`, `spec`) were written during
planning. This card **proves they load** and fills what depended on the repo existing (real
commands, links).

## What to do

- Check that `CLAUDE.md` starts with `@AGENTS.md` and that `GEMINI.md` is a symlink to
  `AGENTS.md`.
- Fill the "Commands" section of `AGENTS.md` with the real scripts from M0-01/M0-03.
- Restart Claude Code in the folder and list the agents: the eight project agents appear, besides
  the machine's.
- The six skills appear in the skill list and each `SKILL.md` references modules and paths that
  exist.
- `## Quality targets` in `CLAUDE.md` with the app's targets (WCAG 2.2 AA; widths 1024 / 1440 /
  1920, because it is desktop; the renderer's performance budget).

## Done when

- [ ] the session's agent list shows `spec-reviewer` … `desktop-reviewer` — proof: screenshot or output.
- [ ] `@spec-reviewer` invoked on a card returns findings in the agent's format — proof: output.
- [ ] `/liminal` loads and stops at step 1 saying there is no `Ready` card (if there is none yet) — proof: output.
- [ ] `AGENTS.md` has no empty section and no broken link — proof: link check.

## Out of scope

Changing the agents' content beyond paths and commands.

## Risks and questions

- If an agent does not load, the defect is usually the frontmatter (YAML) — check before rewriting.
