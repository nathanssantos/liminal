---
id: M1-03
title: Offline render and determinism
milestone: M1
area: engine
priority: P0
depends_on: [M1-02]
listening: false
issue: 13
sync:
  hash: 77462725cc19bd73
  at: 2026-09-06T16:07:08Z
  status: In review
  statusBy: loop
---

## Context

The soundcheck (two-clocks) depends on rendering offline faster than real time, and the plan's
principle 3 depends on the same document yielding the same audio.

## What to do

- `renderOffline({ score, sampleRate = 48000, createContext }) → { channels: Float32Array[], sampleRate, durationSec }`;
  `createContext` receives `(channels, length, sampleRate)` and returns an `OfflineAudioContext` —
  the caller decides whether it is Chromium or the polyfill, **and installs that implementation's
  globals**: Tone tests `instanceof` against the global `OfflineAudioContext` (`rules.md › engine`).
- `encodeWav(channels, sampleRate) → Uint8Array` (16-bit PCM), no dependency.
- `measureBasic(channels) → { peak, rms, silent }` for tests — not the M2 measurement.
- Record in `measurements.md`: render time of the fixture in Node.
- Tests render only as far as they look (`rules.md › engine`): the fixture once, for duration and
  levels; two bars for determinism, the seed and the wav.

## Done when

- [ ] rendered duration = `scoreLengthTicks / PPQ × 60 / bpm` ± 1 sample — proof: test.
- [ ] two renders of the fixture in Node yield **identical** `Float32Array`s — proof: byte-for-byte test.
- [ ] a render with a different `seed` on the same document yields the same audio (the seed does not change notes already written) — proof: test.
- [ ] `peak ≤ 1.0` and `rms > 0.01` on the fixture — proof: test.
- [ ] `encodeWav` produces a valid RIFF header that `ffprobe` reads with the right duration — proof: test calls `ffprobe` if available, otherwise validates the header.
- [ ] the fixture's render time in Node is recorded against `SOUNDCHECK_BUDGET` — proof: line in `measurements.md`, with the budget computed from `two-clocks.md`. **Node does not clear it.** ⚠️ The criterion first read "< 5 s on an M3 Pro", and a first rewrite read "under a third of its own duration", which was wrong twice over: `SOUNDCHECK_BUDGET` is a third of `BRAIN_DEADLINE`, the wall time until `committedEnd − MIN_HORIZON` — 8 bars of slack, 15 s at 128 BPM, so **~5 s for render plus measurement**, not a third of the segment. Measured on an M3 Pro: **6.6–6.9 s** for a 16-bar segment, so Node misses the budget by about 35%, and the cost is the synthesis itself (4 s of the fixture renders in 192 ms with notes, 48 ms without). The soundcheck does not run in Node — it runs in the hidden window (`architecture.md`), and M1-05 measures Chromium. That measurement is the gate; this card records the gap rather than hiding it.

## Out of scope

Chromium × Node comparison (M1-05). Spectral measurement (M2). Whether a bounce carries the
release tail the engine plays after the last tick: the render stops at the last tick, and #65
decides before M3.

## Risks and questions

- If Tone.js schedules with a `Transport` over `OfflineAudioContext` such that a second run in the
  same process inherits state, the determinism test catches it — and the fix is one `Transport`
  per context, never a global `Tone.setContext`.
