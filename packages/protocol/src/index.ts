export type { Channel, ChannelInput, ChannelOutput, Direction } from './channel.ts'
export { defineChannel } from './channel.ts'
export type { EnginePosition, OutputDevices } from './channels.ts'
export {
  CHANNELS,
  engineError,
  OUTPUT_GAIN_DB,
  outputDevice,
  outputMute,
  outputVolume,
  SAFE_OUTPUT_GAIN_DB,
  scoreLoad,
  transportPlay,
  transportPosition,
  transportStop,
} from './channels.ts'
